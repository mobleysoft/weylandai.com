{
  "name": "weylandai-com-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/worker-entry.js --bundle --format=esm --platform=neutral --target=es2022 --outfile=weyland.worker.js --external:cloudflare:workers --external:node:async_hooks"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "1.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
